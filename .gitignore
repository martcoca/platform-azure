config/local/
*.tfstate
*.tfstate.*
.terraform/
.terraform.tfstate.lock.info
crash.log
*.tfplan
*.plan.json

# Bootstrap-only backend override; see README "First run".
*_override.tf
!config/example/backend_bootstrap_override.tf

# CI checks out the pinned cost-guard release here; it is never committed.
.cost-guard-release/

# Doctrine forbids committing these. Enforced by scripts/check-ignore-contract.sh
# after a saved plan carrying an OAuth client secret reached a public repository.
runtime/
*.tfvars
